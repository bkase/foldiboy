# Testing architecture

## Testing lookups and constraints

Fortunately, most Gameboy opcodes have 1 byte as input, and 1 byte as input. That means we can brute-force all 256×256 possibilities to ensure it passes/rejects on all possible inputs
