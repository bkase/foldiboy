[package]
name = "cpu"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = []

[dependencies]
memory = { path = "../memory" }

[dev-dependencies]
apu = { path = "../apu" }

[lints]
workspace = true

