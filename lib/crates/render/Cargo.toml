[package]
name = "render"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
wit-bindgen.workspace = true
cpu = { path = "../cpu" }
memory = { path = "../memory" }
ppu = { path = "../ppu" }
apu = { path = "../apu" }

[lib]
crate-type = ["cdylib"]
